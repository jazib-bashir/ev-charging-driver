/**
 * @deprecated Import `theme` from `@/theme` instead.
 * Kept for backward compatibility with existing components.
 */
import { theme } from '@/theme';

export const GridFlowColors = theme.colors;
