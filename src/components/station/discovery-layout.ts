/**
 * Shared discovery / home layout rhythm.
 * Keep edge inset and section gaps consistent across header → list.
 */
export const DISCOVERY_LAYOUT = {
  edge: 20,
  sectionGap: 12,
  cardGap: 14,
  searchHeight: 45,
  chipHeight: 30,
} as const;
